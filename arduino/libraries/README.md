# Bundled Arduino libraries

The ST7796S projects use the LCDWIKI GUI and LCDWIKI SPI libraries in this
directory. They are kept here so Arduino CLI builds do not depend on a separate
Arduino IDE library installation.

These are upstream third-party sources. Their license and attribution files are
included inside each library directory.
