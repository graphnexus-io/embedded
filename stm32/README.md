# STM32 Projects

STM32 firmware projects and experiments used in demonstrations.

Add each project as a separate subfolder with its own `README.md`, build instructions, flash/debug steps, required STM32Cube or toolchain version, and notes about the exact board used in the video.
